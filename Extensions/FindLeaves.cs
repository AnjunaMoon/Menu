using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using Menu.Models;

namespace Menu.Extensions
{
    public static class MenuItemExtensions
    {
        //public static MenuItem FindItem(this Menu.Models.MenuItem item, string id)
        //{
        //    if (item.Id == id)
        //        return item;
        //    else
        //    {
        //        foreach (var child in item.Items)
        //        {
        //            foreach (var subchild in child.FindItem())
        //            {
        //                return subchild;
        //            }
        //        }
        //    }
        //}
    }
}
